package com.walletline.di

import org.koin.dsl.module

/**
 * Repository module
 *
 * Data related dependencies e.g. data sources, repositories, use cases
 */
val repositoryModule = module {
//    single { repository() }
//    factory { useCases() }
}